# Health check endpoint for nginx
server {
    listen 80;
    server_name localhost 127.0.0.1;

    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}
