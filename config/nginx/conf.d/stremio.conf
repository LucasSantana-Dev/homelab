# Stremio Server Configuration
# NOTE: This file is disabled because Stremio is configured in tailscale-domains.conf
# The configuration in tailscale-domains.conf uses the correct Docker service name (stremio:11470)
# instead of 127.0.0.1:11470 which doesn't work in Docker networking.
#
# This file is kept for reference but disabled to avoid conflicts.
# If you need to modify Stremio nginx configuration, edit tailscale-domains.conf instead.
