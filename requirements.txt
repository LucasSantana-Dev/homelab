# Homelab Manager Dependencies
# This file exists for compatibility with pip install -r requirements.txt
# The canonical source is pyproject.toml
#
# For development, use: pip install -e ".[dev]"
# For production, use:  pip install -e .

# Core dependencies (kept for backward compatibility)
typer[all]>=0.12.0
rich>=13.8.0
docker>=7.1.0
requests>=2.32.0
pyyaml>=6.0.0
psutil>=5.9.0
