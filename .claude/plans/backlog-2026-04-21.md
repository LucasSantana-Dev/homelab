---
repo: LucasSantana-Dev/homelab
branch_at_scan: docs/compose-consolidation
default_branch: main
scan_date: 2026-04-21
scanner: Backlog + Roadmap Map (ultraplan)
---

# Homelab Backlog + Roadmap Map — 2026-04-21

## Scope & prior art

- **Repo in scope:** `LucasSantana-Dev/homelab` (single repo — `homelab-ai-worker/` is a 4-file script dir, not a repo; excluded).
- **Canonical prior art honoured:**
  - `docs/roadmap.md` — auto-generated from `docs/specs/`. Currently shows **1 proposed** item and **2 shipped**.
  - `docs/specs/2026-04-15-script-secret-hygiene/` — open P0 security spec.
  - `docs/specs/2026-04-15-k3s-zombie-cleanup/` — shipped, PR #39.
  - `docs/audits/2026-04-19-service-audit.md` — 8 consolidation candidates waiting on live data.
  - `docs/operations/compose-consolidation.md` — operational playbook landed today (branch of this scan).
  - `docs/adr/0001–0004` — Compose-vs-k3s boundary decisions (k3s dropped).
- **Signals unavailable / skipped:**
  - No git tags → no release-drift metric.
  - No open issues (`gh issue list` returned `[]`).
  - Churn data is shallow (default branch recently rewritten via k3s teardown + consolidation PRs; most files show ≤1 change/90d outside archive).

## State of play (top-line)

| Signal | Value | Note |
|---|---|---|
| Open PRs | 4 | All **GREEN CI** + **MERGEABLE**, all authored by repo owner |
| Open issues | 0 | |
| Failing workflows | **2/6** | `Main CI Watchdog` 3/3 failures, `PR Health Watchdog` 4/4 — both reference a deleted repo |
| Dependabot alerts | 0 open | All 5 historical alerts already `fixed` |
| TODO/FIXME hits | 3 | In 2 files — very clean |
| Skipped tests | 0 | |
| Docs freshness | fresh | README/CHANGELOG/DEVELOPMENT touched 2026-04-21 |
| Commit cadence (180d) | 50 commits | Active: heavy bursts on 2026-03-14/15, 2026-04-14–21 |

---

## A. Now — this week, ≤1 PR each

### A1. Merge the 4 green, mergeable PRs

Four stacked PRs from the repo owner, all with passing CI. Each is the trailing half of the compose-project consolidation work. Merging #43→#46 in order clears the branch stack and lets `main` carry the consolidated state.

| # | Title | Branch | Status | Effort |
|---|---|---|---|---|
| 43 | `chore: remove forge-space.yml (org wound down)` | `chore/remove-forge-space` | GREEN + MERGEABLE | S |
| 44 | `fix: include lan-proxy.yml in top-level compose` | `fix/add-lan-proxy-include` | GREEN + MERGEABLE | S |
| 45 | `fix: extend homelab-docker timeout and pre-pull images` | `fix/systemd-timeout-prepull` | GREEN + MERGEABLE | S |
| 46 | `docs: add compose project consolidation playbook` | `docs/compose-consolidation` (this branch) | GREEN + MERGEABLE | S |

- **Next action:** `gh pr merge 43 --squash --auto && gh pr merge 44 --squash --auto && gh pr merge 45 --squash --auto && gh pr merge 46 --squash --auto` — or merge oldest-first manually to catch any stale-base rebase.
- **Scoring:** [impact: high] [effort: S] [risk: low] [confidence: high]

### A2. Fix broken scheduled watchdogs — remove `Craftvaria` from REPOS_CSV

Both `Main CI Watchdog` and `PR Health Watchdog` fire every 30 minutes and **fail on every run** because they probe `LucasSantana-Dev/Craftvaria`, which 404s (`GraphQL: Could not resolve to a Repository`). Current noise: 48 failure runs/day across the two workflows, plus misleading red badges on the default branch.

- **Evidence:**
  - `.github/workflows/pr-health-watchdog.yml:33` — hardcoded `REPOS_CSV: "LucasSantana-Dev/homelab,LucasSantana-Dev/Lucky,LucasSantana-Dev/Craftvaria"`.
  - `scripts/maintenance/main-ci-watchdog.sh` embeds the same repo list (verify and prune).
  - Run-log snippet: `failed to get runs: HTTP 404 ... /repos/LucasSantana-Dev/Craftvaria/actions/runs` → script aborts.
- **Next action:** single PR — delete `Craftvaria` from `REPOS_CSV` in `pr-health-watchdog.yml` and from the repo-list in `main-ci-watchdog.sh`. Bonus: move the list to a repo-var / env so it's not duplicated.
- **Scoring:** [impact: med] [effort: S] [risk: low] [confidence: high]

### A3. Land the `script-secret-hygiene` spec (P0 security)

Only non-shipped spec in `docs/roadmap.md`. Tagged `security,audit-p0`. The spec dir exists at `docs/specs/2026-04-15-script-secret-hygiene/` but contents are empty (my read returned no content) — **writing the spec body is the first subtask**.

- **Evidence:** `docs/specs/2026-04-15-script-secret-hygiene/` (empty spec); `docs/roadmap.md` line 7.
- **Next action:** draft `spec.md` + `tasks.md` describing the concrete scripts to harden; then ship per-script PRs. Recent precedent: PR #18 (`fix(scripts): source Lucky DB password from .env instead of hardcoding`) and PR #13 (`chore(scripts): enable strict mode across 12 scripts`) fit the pattern.
- **Scoring:** [impact: high] [effort: M] [risk: low] [confidence: high]

---

## B. Next — 2 weeks

### B1. Execute the 2026-04-19 service audit against live host

The audit lays out 8 named candidates (netdata, nginx, whats-up-docker, blackbox↔uptime-kuma, loki+promtail, filebrowser, paperless-ngx, dev-dashboard) but marks itself **"live runtime data pending — homelab unreachable at audit time."**

- **Evidence:** `docs/audits/2026-04-19-service-audit.md` — "Live data" section has 4 placeholder blocks awaiting `scripts/audit-services.sh` output.
- **Next action:** `ssh homelab 'bash -s' < scripts/audit-services.sh > docs/audits/$(date +%F)-live.log`; fill Live-data sections; decide per service.
- **Scoring:** [impact: high] [effort: S] [risk: low] [confidence: high]

### B2. Follow-up PR wave: remove confirmed-stale services

Queued in the audit doc's "Follow-up PRs" list. Each is a narrow compose-file diff once B1 confirms.

- **Next action:** one PR each —
  - `chore(monitoring): remove netdata` (duplicates Prom + node-exporter)
  - `chore(core): remove nginx` (Caddy is LAN edge)
  - `chore(core): remove whats-up-docker` (Renovate handles images)
  - `chore(core): remove filebrowser` (Nextcloud covers it)
  - `chore(monitoring): consolidate blackbox ↔ uptime-kuma`
  - `chore(monitoring): remove loki+promtail` (conditional)
  - `chore(apps): restore-or-remove paperless-ngx` (conditional)
  - `chore: archive dev-dashboard` (conditional)
- **Scoring:** [impact: high] [effort: M] [risk: med] [confidence: med]  (depends on B1 outcome)

### B3. Drop `pytest continue-on-error` — let tests gate CI

`.github/workflows/ci.yml:79` has `continue-on-error: true` on the pytest step. Tests can fail silently while CI reports green. Combined with `fail_ci_if_error: false` on the Codecov upload, coverage regressions also pass unnoticed.

- **Evidence:** `ci.yml:78-81` and `ci.yml:86`.
- **Next action:** remove `continue-on-error` from the pytest step; confirm current test suite is green; add a hard-fail gate on coverage drop.
- **Scoring:** [impact: med] [effort: S] [risk: low] [confidence: high]

### B4. Drop Python 3.9 from CI matrix

Python 3.9 reached EOL on **2025-10-05** (6+ months ago). `pyproject.toml` still declares `requires-python = ">=3.9"` and `ci.yml` runs the whole matrix 3.9/3.10/3.11/3.12 — that's 25% of test-job CPU burned on a dead runtime.

- **Evidence:** `pyproject.toml:11` and `ci.yml` `matrix.python-version` list.
- **Next action:** bump `requires-python = ">=3.10"`, remove 3.9 from matrix, drop the classifier line. Verify no syntax depends on 3.9-only behaviour (none expected — project uses typer/rich/docker stack).
- **Scoring:** [impact: low] [effort: S] [risk: low] [confidence: high]

---

## C. Bug Fixes / Incidents

### C1. Scheduled-workflow red-board — see A2

Same root cause (deleted `Craftvaria` repo). Listed here for completeness: the failing runs show on the repo homepage red-dot and in `gh run list`, polluting any health dashboard.

- **Evidence:** `Main CI Watchdog` 3/3 failures; `PR Health Watchdog` 4/4 failures; all other workflows 0% failures in last ~40 runs.

### C2. Main CI Watchdog retry loop wastes compute

Even after A2, the watchdog retries 4× per failed `gh api` call with exponential backoff (2s → 4s → 8s). Any real outage will burn ~14s per repo before surfacing the failure. Low-cost tune: cap retries at 2, or only retry on 5xx.

- **Evidence:** run log `gh command failed (attempt 1/4), retrying in 2s` pattern for every transient 404.
- **Next action:** patch `scripts/lib/github-api.sh` retry policy.
- **Scoring:** [impact: low] [effort: S] [risk: low] [confidence: med]

---

## D. Tech Debt

### D1. `continue-on-error: true` on pytest — see B3

Listed here for debt framing: test-failure silencing is a persistent time-bomb. Removing it is also a B-next item because the fix is trivial.

### D2. Trivy SARIF upload swallows failures

`ci.yml:182` and `ci.yml:278` both have `continue-on-error: true` on `codeql-action/upload-sarif@v3`. Justifiable (GitHub Security API transient outages shouldn't fail CI), **but** means we'd never know if SARIF uploads have been broken for weeks. Mitigation: add a nightly workflow that asserts the GitHub Security dashboard has a scan result newer than 7 days.

- **Scoring:** [impact: low] [effort: S] [risk: low] [confidence: med]

### D3. `Craftvaria` repo references are likely scattered

Two workflows already reference a repo that doesn't exist. There may be more references in `scripts/`, `docs/`, or skills.

- **Next action:** `grep -rn Craftvaria .` across the repo as part of A2.
- **Scoring:** [impact: low] [effort: S] [risk: low] [confidence: high]

### D4. Missing `CONTRIBUTING.md` / `CODE_OF_CONDUCT.md`

`docs/public-release-hardening.md` exists as a checklist but two of the community-health files it demands are missing. `.github/SECURITY.md` is already present (linked from `README.md`), so only `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` are gaps. Matters if the repo stays public (it is public today).

- **Evidence:** `ls .github/CONTRIBUTING.md .github/CODE_OF_CONDUCT.md` → both ENOENT. `.github/SECURITY.md` → present.
- **Next action:** one PR with two files — copy minimal templates, link to existing `.github/SECURITY.md` and `docs/public-release-hardening.md`.
- **Scoring:** [impact: low] [effort: S] [risk: low] [confidence: high]

---

## E. Refactor Opportunities

### E1. Compose-file consolidation — landed today, confirm on live host

Today's PR #46 + playbook at `docs/operations/compose-consolidation.md` resolves the dual-project collision (`compose` vs `homelab` project names). The playbook is docs-only; the actual `docker compose -p compose down` + `systemctl restart homelab-docker.service` has to run on the homelab host.

- **Next action:** execute the playbook; paste output + `docker compose -p homelab ps` result back into the runbook as "Verified YYYY-MM-DD".
- **Scoring:** [impact: high] [effort: S] [risk: med] [confidence: high]

### E2. Reverse-proxy unification — finish the nginx→caddy cutover

Last shipped as PR #34 (`feat(caddy): unify reverse proxy — cloudflared → caddy-lan → k3s`) and PR #37 (`docs: retire lingering nginx-proxy references`). The audit still flags `nginx` in `core.yml` as live. Removing it closes the loop.

- **Evidence:** audit item 2 in `docs/audits/2026-04-19-service-audit.md`; audit confirms "`config/nginx/conf.d/stremio.conf` was just deleted."
- **Next action:** `chore(core): remove nginx` (bucket B2).
- **Scoring:** [impact: med] [effort: S] [risk: med] [confidence: high]

### E3. Scripts-dir shape — pull hot fixers up, push stale diagnostics to archive

`scripts/` has 10 `.sh` at the root (many `diagnose-*`, `fix-*`). Contract is unclear: some are break-glass tools (keep), some are one-shot fixes (archive after use). Candidate filters:

- `fix-tailscale-exit-node.sh`, `fix-tailscale-routes.sh` — one-shot; last used in a fixed-and-forgotten flow?
- `fix-nextcloud-trusted-domains.sh` — check if runbook still needs it.
- `setup-device-dns.sh`, `test-device-connectivity.sh` — keep as onboarding tools.

- **Next action:** audit `scripts/*.sh` root-level files; move anything that was a one-shot into `archive/scripts-oneshot/`.
- **Scoring:** [impact: low] [effort: S] [risk: low] [confidence: med]

---

## F. Performance / Optimization

### F1. Reclaim ~200MB RAM by removing `netdata` (bucket B2)

Audit: "Netdata pulls ~200MB RAM + continuous CPU … duplicates both collection and UI" relative to Prometheus/node-exporter/cAdvisor/Grafana already running. Needs baseline: `docker stats netdata` over 1h. Target: `netdata` gone, free RAM ≥150MB, no dashboard regressions.

- **Scoring:** [impact: med] [effort: S] [risk: low] [confidence: med]

### F2. Reclaim compute by removing `whats-up-docker`

Renovate already handles image-version updates for `compose/*.yml` via digest pinning (see `renovate.json` packageRules). WUD duplicates this detection with its own scan loop.

- **Next action:** confirm no Homepage card / notification channel subscribes to WUD; remove (bucket B2).
- **Scoring:** [impact: low] [effort: S] [risk: low] [confidence: high]

### F3. Trim CI matrix (bucket B4)

Removing Python 3.9 drops one of four test legs → ~25% less CI compute for the `test` job. Needs baseline: not measured; trivial win.

- **Scoring:** [impact: low] [effort: S] [risk: low] [confidence: high]

---

## G. Security

### G1. `script-secret-hygiene` P0 spec — see A3

Only open security-tagged item in the roadmap. No new advisories surfaced this scan; this is the outstanding remediation from prior audit.

### G2. Verify `.sops.yaml` age-key is a **real** production key, not a placeholder

`.sops.yaml` line 2 shows `age1pljvlg2j8k3nqp7w8xjt9kn2m3q4r5s6t7u8v9w0x1y2z3a4b5c6d7e8f` — this pattern (`a1b2c3...`) is suspiciously sequential. Could be:
  - a real age public key (age pubs *do* look like this) — **OK, it's a pubkey, not a private key**, but
  - check: decrypt an existing `.enc.yaml` with the matching private key. If the key never worked, no file has ever been SOPS-encrypted in this repo → illusion of security.

- **Next action:** `sops -d .env.enc.yaml` if one exists, or encrypt a dummy file round-trip. Grep `find . -name '*.enc.yaml'`.
- **Scoring:** [impact: high] [effort: S] [risk: low] [confidence: med]

### G3. No CodeQL findings triaged (⇒ baseline unknown)

CodeQL workflow passes green but I didn't pull its Security-tab findings. There may be suppressed alerts.

- **Next action:** `gh api /repos/LucasSantana-Dev/homelab/code-scanning/alerts --jq 'group_by(.state)|map({state:.[0].state,count:length})'`.
- **Scoring:** [impact: low] [effort: S] [risk: low] [confidence: low]

### G4. Dependabot: clean — already fixed

All 5 historical Dependabot alerts (`Pygments`, `requests` x3, `black`) are in state `fixed`. No action.

---

## H. Major-Version Migrations (deferred)

- **Python 3.9 → 3.10 min** (see B4/F3). Blocked only by intent, not compatibility. Currently costing CI time.
- **No other major-version hold-outs detected.** Renovate is active with auto-merge for patches/digests and grouped major-only PRs; the fact that no major PR is pending suggests the stack is current.

---

## I. Documentation

### I1. Roadmap is empty — promote 3–5 items out of this backlog

`docs/roadmap.md` shows `_(none)_` under "Now (active)" and 1 proposed. If the Agent-OS spec flow (PR #17) is the source of truth, this backlog should feed it.

- **Next action:** create `docs/specs/2026-04-21-<slug>/` for A2, A3, B1/B2, B3. Promote the Top-5 into the roadmap.
- **Scoring:** [impact: med] [effort: S] [risk: low] [confidence: high]

### I2. Missing community-health files — see D4

### I3. Docs/audit inventory overlap

Both `docs/audit/` (7 files: compose, kubernetes, python, scripts, security, updates, README) and `docs/audits/` (1 file: 2026-04-19-service-audit) exist. Two sibling dirs with near-identical names is a landmine.

- **Next action:** rename one (probably `docs/audit/` → `docs/standards/` since its contents are evergreen quality checklists; keep `docs/audits/` for dated snapshots).
- **Scoring:** [impact: low] [effort: S] [risk: low] [confidence: med]

---

## J. Out of Scope / Won't Fix

- **k3s / kubernetes reintroduction** — ADR 0004 decides it's dropped; `archive/k8s-dropped/` preserves the snapshot.
- **Release tagging / semver** — project is a personal homelab; no downstream consumers. Not worth the ceremony unless `homelab-manager` Python package is published externally.
- **Monorepo restructure** — single app, no pain signal.

---

## Top 10 (impact ÷ effort, ready this fortnight)

| # | Item | Bucket | Effort | Impact | Why now |
|---|---|---|---|---|---|
| 1 | Merge PRs #43–#46 | A1 | S | high | All green, all mergeable; unclogs the branch stack |
| 2 | Strip `Craftvaria` from watchdogs | A2 | S | med | Kills 48 failure runs/day of red-board noise |
| 3 | Run `scripts/audit-services.sh` on host + fill Live-data | B1 | S | high | Unblocks the 8-PR consolidation wave |
| 4 | Write `script-secret-hygiene` spec body + PR | A3 | M | high | Only open P0; spec dir is currently empty |
| 5 | Remove `netdata` | B2/F1 | S | med | ~200MB RAM reclaimed; Prom stack already covers it |
| 6 | Remove `continue-on-error` on pytest | B3 | S | med | Tests can silently fail right now |
| 7 | Remove `nginx` from `core.yml` | B2/E2 | S | med | Finishes the Caddy cutover |
| 8 | Drop Python 3.9 from CI matrix | B4 | S | low | 25% less CI compute, 3.9 is EOL |
| 9 | Verify `.sops.yaml` age key round-trips | G2 | S | high | "Encryption that doesn't decrypt" is worse than plain text |
| 10 | Add `CONTRIBUTING.md` + `SECURITY.md` + `CODE_OF_CONDUCT.md` | D4/I2 | S | low | Repo is public; `public-release-hardening.md` already demands them |

## Critical findings surfaced

- **Security latent risk:** pytest `continue-on-error: true` means test regressions don't fail CI (B3).
- **Security latent risk:** verify SOPS age key actually works — the key in `.sops.yaml` follows a pattern that *could* be a placeholder (G2).
- **CI noise:** two scheduled watchdogs failing every 30 min because of a deleted sibling repo — every red badge you see is false (A2).
- **Stuck security spec:** the only P0 roadmap item has an empty spec directory (A3).

## Suggested cadence

- Re-run this backlog map every 2 weeks (next: 2026-05-05). Dated filename gives diffable history.
- Pair with the Agent-OS spec flow (PR #17) — promote Top-5 into `docs/specs/2026-04-21-<slug>/` before the next run.

---

## Execution log (added post-scan)

During the same session the backlog was written, the following was shipped or opened as PRs against `main`:

| Item | Outcome | Ref |
|---|---|---|
| A1 merge #43 | **MERGED** | https://github.com/LucasSantana-Dev/homelab/pull/43 |
| A1 merge #44 | **MERGED** | https://github.com/LucasSantana-Dev/homelab/pull/44 |
| A1 merge #45 | **MERGED** | https://github.com/LucasSantana-Dev/homelab/pull/45 |
| A1 merge #46 | branch updated, auto-merge pending CI | https://github.com/LucasSantana-Dev/homelab/pull/46 |
| A2 strip Craftvaria + B4 drop py3.9 + B3 gate pytest | PR opened | https://github.com/LucasSantana-Dev/homelab/pull/47 |
| D4 community-health files | PR opened | https://github.com/LucasSantana-Dev/homelab/pull/48 |
| A3 script-secret-hygiene spec body | PR opened | https://github.com/LucasSantana-Dev/homelab/pull/49 |
| G2 SOPS age key verification | PR opened | https://github.com/LucasSantana-Dev/homelab/pull/50 |
| This backlog doc | PR opened | (self) |

Deviations from the original plan:

- **B3 (remove pytest continue-on-error) was partially reverted.** Removing the flag exposed that `tests/unit/test_cli.py`, `test_container_manager.py`, `test_health.py`, and `test_updates.py` reference a `HomelabAutomation` / `HomelabCLI` API that no longer exists (refactored out of the current typer CLI). Those tests need to be rewritten before pytest can actually gate CI. PR #47 kept `continue-on-error: true` with an inline comment flagging the follow-up.
- **G2 findings were worse than expected.** The age key in `.sops.yaml` isn't just stale — it's a fabricated string (`age1pljvlg2j8k3nqp…`) that the `age` CLI refuses as `invalid recipient encoding`. No `.enc.yaml` files exist, so SOPS has never run against it. PR #50 replaces with an obvious placeholder.
- **A3 spec-secret-hygiene is narrower than expected.** PRs #18 and #43 already did most of the live-code work; remaining tasks are (a) delete dead `FORGE_MCP_*` guard from `update-containers.sh`, (b) rotate the historically-leaked Lucky DB password, (c) decide on `git filter-repo` scrub, (d) add a gitleaks rule for inline shell secrets. PR #49 documents this.

New follow-up items surfaced during execution (not in original buckets):

- **F4 Rewrite broken test classes.** `test_cli.py` + 3 others reference the removed `HomelabAutomation`/`HomelabCLI` API. Blocks pytest gating. Scope: rewrite against `homelab_manager.cli.commands.create_app` (typer). [impact: med] [effort: M] [risk: low] [confidence: high]
- **D5 Dedupe `docs/audit/` vs `docs/audits/`.** Still valid observation from I3 — two sibling dirs with near-identical names. Not acted on this pass.
