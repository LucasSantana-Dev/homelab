apiVersion: v1
kind: LimitRange
metadata:
  name: defaults
  namespace: observability
spec:
  limits:
    - type: Container
      default:
        cpu: "300m"
        memory: 256Mi
      defaultRequest:
        cpu: "100m"
        memory: 128Mi
---
apiVersion: v1
kind: LimitRange
metadata:
  name: defaults
  namespace: apps
spec:
  limits:
    - type: Container
      default:
        cpu: "500m"
        memory: 512Mi
      defaultRequest:
        cpu: "150m"
        memory: 192Mi
