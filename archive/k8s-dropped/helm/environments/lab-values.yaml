# Shared overrides for the 90-day migration lab.
global:
  edgeMode: compose-nginx-cloudflared
  ingressClassName: traefik

homepage:
  ingress:
    host: home.k3s.local

blackbox-exporter:
  ingress:
    host: blackbox.k3s.local

filebrowser:
  ingress:
    host: files.k3s.local
